name: flutter_mcp_widgets
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.2.5

dependencies:
  flutter:
    sdk: flutter
  collection:
  flutter_markdown:
  uuid:
  flutter_svg:

  flutter_mcp_entities:

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.1.0
  test: ^1.24.0
